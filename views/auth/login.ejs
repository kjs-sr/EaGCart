<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>로그인 | EaGCart</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-slate-50 min-h-screen flex flex-col">
    <main class="flex-1 flex flex-col items-center justify-center px-6 py-12">
      <!-- 로고 영역 -->
      <div class="mb-8">
        <a href="/" class="hover:opacity-80 transition-opacity">
          <img
            src="/images/logo.png"
            alt="EaGCart 로고"
            class="h-28 w-auto object-contain"
          />
        </a>
      </div>

      <section
        class="w-full max-w-md bg-white rounded-3xl shadow-xl p-8 sm:p-10"
      >
        <h1 class="text-2xl font-bold text-slate-800 text-center mb-8">
          로그인
        </h1>

        <!-- form action을 /auth/login으로 설정 -->
        <form class="space-y-5" action="/auth/login" method="POST">
          <div>
            <input
              type="text"
              name="userId"
              placeholder="아이디"
              class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-5 py-4 text-sm focus:bg-white focus:border-purple-500 focus:outline-none transition-colors"
              required
            />
          </div>
          <div>
            <input
              type="password"
              name="password"
              placeholder="비밀번호"
              class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-5 py-4 text-sm focus:bg-white focus:border-purple-500 focus:outline-none transition-colors"
              required
            />
          </div>

          <button
            type="submit"
            class="w-full rounded-2xl bg-purple-600 py-4 font-bold text-white tracking-wide hover:bg-purple-700 transition shadow-md mt-2"
          >
            로그인
          </button>
        </form>

        <div
          class="mt-8 flex items-center justify-center gap-6 text-sm font-semibold text-purple-600"
        >
          <a href="/register" class="hover:text-purple-800 transition-colors"
            >회원가입</a
          >
          <span class="h-3 w-px bg-slate-300"></span>
          <a
            href="/account/recovery"
            class="hover:text-purple-800 transition-colors"
            >아이디/비밀번호 찾기</a
          >
        </div>
      </section>
    </main>
    <%- include('../partials/footer.ejs') %>
  </body>
</html>
